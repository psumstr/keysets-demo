import * as React from 'react';
import Navbar from './Navbar';
import WidgetGrid from './WidgetGrid';

const App = () => {
  return (
      <div>
          <Navbar />
          <WidgetGrid />
      </div>
  )
};

export default App;
