import * as React from "react";

const Navbar = () => {
    return (
        <nav className="pt-navbar pt-dark">
            <img src={require('../images/zoomdataLogo.svg')}/>
        </nav>
    );
};


export default Navbar;
