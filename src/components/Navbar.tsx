import * as React from "react";
import * as image from '../images/zoomdataLogo.svg';

const Navbar = () => {
    return (
        <nav className="pt-navbar pt-dark">
            <img src={image}/>
        </nav>
    );
};

export default Navbar;
